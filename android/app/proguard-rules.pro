# Empty on purpose: the Flutter Gradle plugin and the app's plugins
# (device_calendar_plus included) ship their own consumer ProGuard rules,
# which are merged automatically. Add app-specific keep rules here only if
# a future release build strips something it shouldn't.
