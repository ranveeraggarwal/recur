import 'package:flutter/widgets.dart';

/// Opens the calendar picker sheet that lets the user choose which writable
/// calendar Recur books into.
///
/// This is a stub so [lib/screens/home/home_screen.dart] compiles; the
/// booking-confirm issue (`docs/architecture.md`, M6) replaces it with the
/// real `showModalBottomSheet`-backed picker.
// TODO(#23): replace with the real calendar picker sheet.
Future<void> showCalendarPicker(BuildContext context) async {}
